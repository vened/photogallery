.mygallery
  .tn3.album
    %h4 Fixed Dimensions
    .tn3.description Images with fixed dimensions
    %ol
      -@photos.each do |photo|
        %li.photo
          =link_to photo.file_url(:large), :class => 'js-open-lyer js-open-photo', :data =>{:index => @index} do
            =image_tag photo.file_url(:thumb)


-#
-#%ul.album.js-album.clearfix
-#  -@index = 0
-#  -@photos.each do |photo|
-#    %li.photo
-#      =link_to photo.file_url(:large), :class => 'js-open-lyer js-open-photo', :data =>{:index => @index} do
-#        =image_tag photo.file_url(:thumb)
-#    -@index = @index + 1